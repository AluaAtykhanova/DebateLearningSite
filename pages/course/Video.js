import { Navbar, ComingSoon, Video } from '../../components';

const Home = () => (
  <div className="bg-primary-black overflow-hidden">
    <Navbar />
    <Video />
  </div>
);

export default Home;
